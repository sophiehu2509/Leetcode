int missingNumber(vector<int>& nums) {
        int i=0;
        int result=0;
        for (i=0;i<nums.size();i++)
            result^=nums[i]^i;
            
            return result^nums.size();
    }
