class Solution {
public:
    bool isPalindrome(int x) {
         int comX = x;
         int point = 0;
         while (x>0)
         {
             point = point * 10 + x % 10;
            
             x = x/10;
         }
         
         return point == comX;
    }
};
